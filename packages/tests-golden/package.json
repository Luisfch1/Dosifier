{
  "name": "@concrete/tests-golden",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json -w"
  },
  "dependencies": {
    "@concrete/engine": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.3",
    "typescript": "^5.6.3"
  }
}